# 热血问答 - 项目功能列表与开发进度

> 本文档基于 [PRD.md](./PRD.md) 与 [可行性方案.md](./可行性方案.md) 深入核对生成，作为项目开发进度跟踪的唯一来源。
> 后续开发每完成一项功能，请直接更新对应条目的状态字段（状态取值：✅ 已完成 / 🟡 部分完成 / ⬜ 未开始）。
>
> **最后更新时间**：2026-07-17（批次 6 F-005 进度保存 + E-002 矩阵题 + F-004 微信 JS-SDK 完成）
> **当前总体进度**：约 **87.1%**

---

## 一、总体进度概览

| 模块 | 总功能数 | 已完成 | 部分完成 | 未开始 | 完成率 |
|------|---------|-------|---------|-------|-------|
| 1. 项目基础设施 | 8 | 8 | 0 | 0 | 100% |
| 2. 用户与认证模块 | 6 | 6 | 0 | 0 | 100% |
| 3. 问卷管理模块 | 8 | 8 | 0 | 0 | 100% |
| 4. 问卷编辑器 | 6 | 5 | 0 | 1 | 83% |
| 5. 模板管理模块 | 5 | 5 | 0 | 0 | 100% |
| 6. 问卷填写模块 | 6 | 6 | 0 | 0 | 100% |
| 7. 数据分析模块 | 6 | 5 | 0 | 1 | 83% |
| 8. 数据可视化 | 8 | 5 | 0 | 3 | 63% |
| 9. 安全与性能 | 7 | 6 | 0 | 1 | 86% |
| 10. 部署与运维 | 6 | 6 | 0 | 0 | 100% |
| 11. 测试与质量保障 | 4 | 2 | 0 | 2 | 50% |
| **合计** | **70** | **61** | **0** | **9** | **87.1%** |

---

## 二、详细功能清单

### 1. 项目基础设施 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| P-001 | 后端项目骨架 | 高 | ✅ | [backend/src/app.ts](../backend/src/app.ts) - Express + 中间件链路完整 |
| P-002 | 前端项目骨架 | 高 | ✅ | [frontend/src/App.tsx](../frontend/src/App.tsx) - React 18 + Vite + React Router |
| P-003 | TypeScript 类型支持 | 高 | ✅ | 前后端均启用 TS（tsconfig.json） |
| P-004 | MongoDB 数据库连接 | 高 | ✅ | [backend/src/config/database.ts](../backend/src/config/database.ts) |
| P-005 | 统一 API 响应格式 | 高 | ✅ | 控制器统一返回 `{ success, data, message }` |
| P-006 | 健康检查接口 | 中 | ✅ | `GET /health` |
| P-007 | 全局错误处理 | 高 | ✅ | [app.ts](../backend/src/app.ts) 404 与 500 中间件 |
| P-008 | API 服务层封装 | 高 | ✅ | [frontend/src/services/api.ts](../frontend/src/services/api.ts) 含拦截器 |

### 2. 用户与认证模块 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| U-001 | 用户注册 | 高 | ✅ | `POST /api/auth/register` ([authController.ts](../backend/src/controllers/authController.ts)) |
| U-002 | 用户登录 | 高 | ✅ | `POST /api/auth/login`，JWT 7天有效期 |
| U-003 | 密码加密存储 | 高 | ✅ | bcryptjs (salt rounds=10) ([User.ts](../backend/src/models/User.ts)) |
| U-004 | JWT 认证中间件 | 高 | ✅ | [backend/src/middleware/auth.ts](../backend/src/middleware/auth.ts) |
| U-005 | 获取当前用户信息 | 中 | ✅ | `GET /api/auth/profile` |
| U-006 | 角色权限控制 | 高 | ✅ | 三角色体系 admin/analyst/editor（保留 user 兼容旧数据，按 editor 处理）：[User.ts](../backend/src/models/User.ts) UserRole 类型；[auth.ts](../backend/src/middleware/auth.ts) requireRoles 工厂函数；[authController.ts](../backend/src/controllers/authController.ts) register 禁止自选 admin、默认 editor；问卷路由读三角色/写 admin+editor；答案路由仅 admin+analyst；22 个 U-006 测试用例（auth.test.ts） |

### 3. 问卷管理模块 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| Q-001 | 问卷列表（分页、搜索、状态筛选） | 高 | ✅ | `GET /api/questionnaires` ([questionnaireController.ts](../backend/src/controllers/questionnaireController.ts)) |
| Q-002 | 问卷详情查询 | 高 | ✅ | `GET /api/questionnaires/:id` |
| Q-003 | 创建问卷 | 高 | ✅ | `POST /api/questionnaires` |
| Q-004 | 更新问卷（仅草稿可编辑） | 高 | ✅ | `PUT /api/questionnaires/:id` |
| Q-005 | 删除问卷 | 中 | ✅ | `DELETE /api/questionnaires/:id` |
| Q-006 | 发布问卷 | 高 | ✅ | `POST /api/questionnaires/:id/publish` |
| Q-007 | 关闭（停用）问卷 | 高 | ✅ | `POST /api/questionnaires/:id/close` |
| Q-008 | 公开问卷访问（无需登录） | 高 | ✅ | `GET /api/questionnaires/public/:id` |

### 4. 问卷编辑器 🟡 83%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| E-001 | 题型支持：单选/多选/填空/评分 | 高 | ✅ | [Questionnaire.ts](../backend/src/models/Questionnaire.ts) type 枚举 |
| E-002 | 矩阵题（matrix）支持 | 中 | ✅ | [Questionnaire.ts](../backend/src/models/Questionnaire.ts) Schema 含 matrixRows/matrixCols 字段（matrix 类型至少 2 行 2 列）；[questionnaireController.ts](../backend/src/controllers/questionnaireController.ts) 转换函数 backendToFrontendTypeMap `'matrix': 'matrix'`（不再降级为 text）、convertFrontendToBackendQuestions/convertBackendToFrontendQuestions 透传 matrixRows/matrixCols、importQuestionnaire validTypes 追加 matrix；[answerController.ts](../backend/src/controllers/answerController.ts) buildQuestionStats 新增 matrix 分支 rowStats 聚合；5 个 E-002 测试用例（questionnaire.test.ts） |
| E-003 | 必填设置 | 高 | ✅ | `required` 字段 + Schema 校验 |
| E-004 | 选项管理（增删改） | 高 | ✅ | 编辑器与转换函数支持 |
| E-005 | 题目排序 | 高 | ✅ | `order` 字段 + 前端编辑器拖拽 |
| E-006 | 逻辑跳转 | 中 | ⬜ | PRD/Schema 已规划 logicJump 字段，代码中无任何实现 |

### 5. 模板管理模块 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| T-001 | 模板数据模型 | 高 | ✅ | [Template.ts](../backend/src/models/Template.ts) |
| T-002 | 预设模板数据 | 中 | ✅ | [data/templates.ts](../backend/src/data/templates.ts) 含献血满意度等模板 |
| T-003 | 获取模板列表 | 高 | ✅ | `GET /api/templates` |
| T-004 | 获取模板详情 | 中 | ✅ | `GET /api/templates/:id` |
| T-005 | 基于模板创建问卷 | 高 | ✅ | `POST /api/templates/:id/create` |

### 6. 问卷填写模块 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| F-001 | 提交问卷答案（公开） | 高 | ✅ | `POST /api/answers/submit` ([answerController.ts](../backend/src/controllers/answerController.ts)) |
| F-002 | 必填校验 | 高 | ✅ | 前端 [QuestionnaireFillPage.tsx](../frontend/src/pages/Public/QuestionnaireFillPage.tsx) 表单校验 |
| F-003 | 移动端响应式适配 | 高 | ✅ | Ant Design 响应式 + 设备检测 |
| F-004 | 微信环境检测 | 高 | ✅ | 前端 UA 检测 isWechatEnv + [wxSdk.ts](../frontend/src/services/wxSdk.ts) 动态加载 jweixin-1.6.0.js + 调用后端签名接口执行 wx.config；后端 [wxController.ts](../backend/src/controllers/wxController.ts) `GET /api/wx/signature` 提供 appId/timestamp/nonceStr/signature，access_token 与 jsapi_ticket 缓存（7200s，提前 300s 刷新），Node 内置 https+crypto 无新依赖；签名算法 SHA1(jsapi_ticket=X&noncestr=X&timestamp=X&url=X) 按字典序；[QuestionnaireFillPage.tsx](../frontend/src/pages/Public/QuestionnaireFillPage.tsx) source 上报修复（微信环境 source='wechat'）；未配置 WX_APPID/WX_APP_SECRET 时 503 降级；8 个 F-004 测试用例（wx.test.ts） |
| F-005 | 进度保存（断点续填） | 中 | ✅ | [QuestionnaireFillPage.tsx](../frontend/src/pages/Public/QuestionnaireFillPage.tsx) localStorage 草稿持久化：key=`questionnaire_draft_${id}`；防抖 500ms 保存（draftTimerRef + Form onValuesChange）；首次加载检测草稿并显示恢复横幅（handleRestoreDraft/handleDiscardDraft）；filterDraftByQuestions 过滤失效题目；提交成功后 clearDraft 清理；预览模式与已提交状态跳过保存 |
| F-006 | 防重复提交 | 高 | ✅ | 后端双层防护：S-005 限流中间件（5次/分钟）+ F-006 业务级防重复提交（同 IP+问卷 10s 窗口）；[answerController.ts](../backend/src/controllers/answerController.ts) submitAnswer 内 DEDUP_WINDOW_MS=10s；[rateLimit.ts](../backend/src/middleware/rateLimit.ts) 自实现内存固定窗口；3 个 F-006 专项测试用例（answer.test.ts） |

### 7. 数据分析模块 🟡 50%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| D-001 | 答案列表查询（分页） | 高 | ✅ | `GET /api/answers/:questionnaireId` |
| D-002 | 答案详情查询 | 中 | ✅ | `GET /api/answers/:questionnaireId/:answerId` |
| D-003 | 基础统计（总数/来源/设备/平均时长） | 高 | ✅ | `GET /api/answers/statistics/:id`，BUG-001 已修复（分支 bugfix/fix-001-statistics-route-0717），S-006 后已含完成率字段 |
| D-004 | 题目级统计分布 | 高 | ✅ | `GET /api/answers/statistics/:id` 返回 `statistics.questionStats`，按题型聚合：单选/多选选项计数+百分比、文本去重计数、评分平均分+分布；[answerController.ts](../backend/src/controllers/answerController.ts) buildQuestionStats；6 个测试用例（answer.test.ts） |
| D-005 | 数据导出（Excel/CSV） | 高 | ✅ | `GET /api/answers/:questionnaireId/export?format=csv\|excel`，[answerController.ts](../backend/src/controllers/answerController.ts) exportAnswers 控制器，CSV 含 UTF-8 BOM；测试 D-005 共 7 用例（answer.test.ts） |
| D-006 | 时间/渠道筛选 | 中 | ⬜ | 接口未支持按时间范围、渠道筛选 |

### 8. 数据可视化 🟡 44%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| V-001 | 来源分布饼图 | 高 | ✅ | [StatisticsPage.tsx](../frontend/src/pages/Admin/StatisticsPage.tsx) ECharts Pie |
| V-002 | 时间趋势折线图 | 高 | ✅ | 同上，ECharts Line |
| V-003 | 统计概览卡片 | 高 | ✅ | Statistic 组件展示总数/完成率/平均时长 |
| V-004 | 题目级图表（柱状图） | 高 | ✅ | [StatisticsPage.tsx](../frontend/src/pages/Admin/StatisticsPage.tsx) QuestionStatCard：选择题柱状图、评分题饼图、文本题列表；修复 api.ts statistics 路径不一致 + fetchStatistics 字段映射 + 题型判断兼容 single/multiple |
| V-005 | 评分题雷达图 | 中 | ⬜ | 未实现 RadarChart |
| V-006 | 填空题词云图 | 中 | ⬜ | 未引入 echarts-wordcloud 扩展 |
| V-007 | 矩阵题热力图 | 中 | ⬜ | 未实现 Heatmap |
| V-008 | 仪表盘 Dashboard 页 | 高 | ✅ | 后端 `GET /api/answers/statistics/dashboard` 跨问卷聚合（[answerController.ts](../backend/src/controllers/answerController.ts) getDashboard），返回 overview/trend/sourceStats/deviceStats/questionnaireStatusStats/topQuestionnaires；前端 [DashboardPage.tsx](../frontend/src/pages/Admin/DashboardPage.tsx) 5 概览卡片 + 4 ECharts 图表 + Top 5 问卷表；[App.tsx](../frontend/src/App.tsx) 新增 dashboard 路由为默认首页；[AdminLayout.tsx](../frontend/src/components/Layout/AdminLayout.tsx) 新增仪表盘菜单项；10 个 V-008 测试用例（answer.test.ts） |

### 9. 安全与性能 🟡 86%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| S-001 | HTTPS 传输加密 | 高 | ✅ | Nginx 配置支持 443 端口 |
| S-002 | Helmet 安全头 | 高 | ✅ | [app.ts](../backend/src/app.ts) helmet 中间件 |
| S-003 | 数据库索引优化 | 高 | ✅ | Questionnaire/Answer/User/Template 均配置索引；Answer 新增 `{ipAddress, questionnaireId, submittedAt}` 复合索引支撑 F-006 |
| S-004 | 敏感数据脱敏 | 高 | ✅ | 密码字段 select:false；Answer.ipAddress 字段存储脱敏 IP，[answerController.ts](../backend/src/controllers/answerController.ts) maskIp 函数：IPv4 末段置 0、处理 ::ffff: 映射格式；3 个 S-004 专项测试用例（answer.test.ts） |
| S-005 | 防重复提交/限流 | 高 | ✅ | [rateLimit.ts](../backend/src/middleware/rateLimit.ts) 自实现内存固定窗口限流（避免引入 express-rate-limit 依赖），submit 路由挂载 5次/分钟；X-RateLimit-* 响应头；1 个 S-005 专项测试用例（answer.test.ts） |
| S-006 | 完成率字段与计算 | 中 | ✅ | [Answer.ts](../backend/src/models/Answer.ts) isCompleted 字段（带索引）；[answerController.ts](../backend/src/controllers/answerController.ts) checkCompletion（所有 required 题目非空）+ isAnswerEmpty（4 种空值类型，数字 0 视为已作答）；submitAnswer 返回 isCompleted；getStatistics 返回 completedAnswers+completionRate（保留 1 位小数）；getDashboard 返回跨问卷 avgCompletionRate；13 个 S-006 测试用例（answer.test.ts） |
| S-007 | 统计结果预计算缓存 | 中 | ⬜ | PRD 提到 statistics 表 + 混合计算模式，未实现 |

### 10. 部署与运维 ✅ 100%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| O-001 | 开发环境 Docker Compose | 高 | ✅ | [docker-compose.yml](../docker-compose.yml) |
| O-002 | 生产环境 Docker Compose | 高 | ✅ | [docker-compose.prod.yml](../docker-compose.prod.yml) |
| O-003 | 前端 Dockerfile | 高 | ✅ | [Dockerfile](../Dockerfile) |
| O-004 | 后端 Dockerfile | 高 | ✅ | [Dockerfile.backend](../Dockerfile.backend) |
| O-005 | Nginx 反向代理配置 | 高 | ✅ | [nginx/](../nginx/) dev + prod 两套配置 |
| O-006 | 部署脚本（Win/Linux） | 中 | ✅ | [scripts/](../scripts/) 4 个部署脚本 |

### 11. 测试与质量保障 🟡 50%

| 编号 | 功能 | 优先级 | 状态 | 说明 / 验证依据 |
|-----|------|-------|------|---------------|
| TQ-001 | 后端单元测试框架 | 高 | ✅ | [jest.config.js](../backend/jest.config.js) + ts-jest + mongodb-memory-server，144 个用例 |
| TQ-002 | 后端 API 接口测试 | 高 | ✅ | [backend/test/](../backend/test/) 5 个测试文件覆盖全部 21 个接口（含导出+题目级统计+防重复+限流+脱敏+仪表盘+角色权限+完成率+矩阵题+微信签名），144 用例全部通过 |
| TQ-003 | 前端组件测试 | 中 | ⬜ | 未配置测试框架 |
| TQ-004 | 端到端测试 | 中 | ⬜ | 未实现 |

---

## 三、与 PRD 对照的差异分析

### 3.1 接口路径差异

PRD 7.2 节定义的接口与实际实现存在以下差异：

| PRD 定义 | 实际实现 | 差异说明 |
|---------|---------|---------|
| `POST /api/users/login` | `POST /api/auth/login` | 路径前缀改为 auth |
| `POST /api/answers` | `POST /api/answers/submit` | 子路径调整 |
| `GET /api/answers/:id` | `GET /api/answers/:questionnaireId/:answerId` | 改为双参数 |
| `GET /api/answers/export` | `GET /api/answers/:questionnaireId/export` | 路径调整为按问卷 ID 导出，支持 csv/excel 格式 |
| `GET /api/questionnaires/:id/publish` (GET) | `POST /api/questionnaires/:id/publish` | 改为 POST（更合理） |
| `GET /api/statistics/:id` | `GET /api/answers/statistics/:questionnaireId` | 路径与模块归属调整 |
| `GET /api/statistics/dashboard` | `GET /api/answers/statistics/dashboard` | 已实现（V-008 批次 4），路径调整到 answers 模块下，跨问卷聚合 |
| `GET /api/statistics/export` | （未实现） | 缺失统计导出接口 |

### 3.2 数据模型差异

| PRD 字段 | 当前模型 | 差异说明 |
|---------|---------|---------|
| Answer.isCompleted | ✅ 已实现 | S-006 批次 5 完成，含 isCompleted 字段+完成率计算 |
| Answer.userId / anonymousId | 未实现 | PRD 要求区分登录用户/匿名用户 |
| Answer.ipAddress | ✅ 已实现 | S-004 批次 3 完成，脱敏末段置 0 |
| statistics 表 | 未实现 | PRD 要求预计算统计结果表 |
| Question.logicJump | 未实现 | 逻辑跳转字段缺失 |
| User.role = admin/analyst/editor | ✅ 已实现 | U-006 批次 5 完成，三角色体系（保留 user 兼容） |

### 3.3 功能缺失汇总（按优先级排序）

**高优先级缺失（影响核心流程）：**
1. ~~仪表盘 Dashboard - 前端页面未实现~~ ✅ 已于批次 4 完成
2. ~~防重复提交 - 前后端均未实现~~ ✅ 已于批次 3 完成
3. ~~角色权限控制 - 仅二角色简化实现~~ ✅ 已于批次 5 完成
4. ~~后端单元测试 - 框架已就绪但无用例~~ ✅ 已完成 131 用例

**中优先级缺失（影响体验与扩展性）：**
7. ~~进度保存/断点续填~~ ✅ 已于批次 6 完成
8. ~~微信 JS-SDK 集成~~ ✅ 已于批次 6 完成
9. 逻辑跳转功能
10. ~~矩阵题完整支持（前后端转换）~~ ✅ 已于批次 6 完成
11. 词云图 / 热力图 / 雷达图
12. 时间/渠道筛选
13. ~~完成率字段与计算~~ ✅ 已于批次 5 完成
14. 统计结果预计算缓存
15. ~~IP 收集与脱敏~~ ✅ 已于批次 3 完成

---

## 四、里程碑达成情况

参考 PRD 6.1 与可行性方案 5.2 节里程碑：

| 里程碑 | 计划时间 | 计划交付物 | 当前状态 |
|-------|---------|----------|---------|
| M1 | 第1周 | 需求文档、技术方案、数据库设计 | ✅ 已完成 |
| M2 | 第5周 | 前端页面开发完成 | 🟡 95%（Dashboard + F-005 草稿恢复 + F-004 微信 SDK 已完成；缺词云/热力图等高级图表） |
| M3 | 第6/8周 | 后端 API 开发完成 | 🟡 98%（导出+题目级统计+防重复+限流+脱敏+仪表盘+角色权限+完成率+矩阵题+微信签名已完成；缺逻辑跳转、筛选） |
| M4 | 第8/10周 | 测试通过，功能完整 | 🟡 73%（后端 API 测试已编写 144 用例，前端/E2E 待补） |
| M5 | 第9/11周 | 上线部署，用户手册 | 🟡 部署已就绪，用户手册未交付 |

---

## 五、变更记录

| 日期 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2026-07-16 | 初版功能列表生成，完成进度核对 | - |
| 2026-07-17 | 批次 1：D-005 数据导出功能（CSV/Excel）完成，新增 `GET /api/answers/:questionnaireId/export` 接口及 7 个测试用例，整体进度 75% → 76.4% | - |
| 2026-07-17 | 批次 2：D-004 题目级统计分布 + V-004 题目级图表完成，统计接口返回 questionStats（按题型聚合），前端 StatisticsPage 接入柱状图/饼图/文本列表，整体进度 76.4% → 77.9% | - |
| 2026-07-17 | 批次 3：F-006 防重复提交 + S-005 限流 + S-004 IP 脱敏完成。新增 [rateLimit.ts](../backend/src/middleware/rateLimit.ts) 内存固定窗口限流（5 次/分钟），submitAnswer 内 DEDUP_WINDOW_MS=10s 业务级防重复；Answer 模型新增 ipAddress 字段（脱敏末段置 0）+ 复合索引；新增 7 个 F-006/S-005/S-004 专项测试用例；BUG-002 同步修复（answer.test.ts 引入 submitAnswer 辅助函数分配唯一 IP）。整体进度 77.9% → 81.4%，M3 85% → 90%，M4 60% → 65% | - |
| 2026-07-17 | 批次 4：V-008 仪表盘 Dashboard 完成。后端新增 `GET /api/answers/statistics/dashboard` 跨问卷聚合接口（getDashboard，6 个聚合管道：overview/trend/sourceStats/deviceStats/questionnaireStatusStats/topQuestionnaires，含近 7 天趋势补全 0 填充）；前端新增 [DashboardPage.tsx](../frontend/src/pages/Admin/DashboardPage.tsx)（5 概览卡片 + 4 ECharts 图表 + Top 5 问卷 Table），App.tsx 新增 dashboard 路由并改为默认首页，AdminLayout 新增仪表盘菜单项（DashboardOutlined）；新增 10 个 V-008 测试用例。整体进度 81.4% → 82.9%，M2 85% → 90%，M3 90% → 93%，M4 65% → 68% | - |
| 2026-07-17 | 批次 5：U-006 角色权限控制 + S-006 完成率字段完成。U-006：User 模型扩展为 admin/analyst/editor（保留 user 兼容旧数据，按 editor 处理）+ UserRole 类型；auth.ts 新增 requireRoles 工厂函数；authController register 禁止自选 admin、默认 editor；问卷路由读三角色/写 admin+editor；答案路由仅 admin+analyst；helpers registerAndLogin 先 editor 注册再 Model 升级目标角色。S-006：Answer 模型新增 isCompleted 字段（带索引）；answerController checkCompletion（所有 required 题目非空）+ isAnswerEmpty（4 种空值类型，数字 0 视为已作答）；submitAnswer 返回 isCompleted；getStatistics 返回 completedAnswers+completionRate；getDashboard 返回跨问卷 avgCompletionRate。新增 22 个 U-006 + 13 个 S-006 测试用例。整体进度 82.9% → 84.3%，M3 93% → 96%，M4 68% → 70% | - |
| 2026-07-17 | 批次 6：F-005 进度保存 + E-002 矩阵题 + F-004 微信 JS-SDK 完成。F-005：[QuestionnaireFillPage.tsx](../frontend/src/pages/Public/QuestionnaireFillPage.tsx) localStorage 草稿持久化（key=`questionnaire_draft_${id}`、防抖 500ms 保存、恢复横幅、filterDraftByQuestions 过滤失效题目、提交清理、预览/已提交跳过保存）。E-002：[questionnaireController.ts](../backend/src/controllers/questionnaireController.ts) 修复转换函数降级（`'matrix': 'matrix'` 不再降级为 text）、convertFrontendToBackendQuestions/convertBackendToFrontendQuestions 透传 matrixRows/matrixCols、importQuestionnaire validTypes 追加 matrix；[answerController.ts](../backend/src/controllers/answerController.ts) buildQuestionStats 新增 matrix 分支 rowStats 聚合。F-004：[wxController.ts](../backend/src/controllers/wxController.ts) `GET /api/wx/signature` 签名接口 + access_token/jsapi_ticket 缓存（7200s，提前 300s 刷新）+ Node 内置 https+crypto 无新依赖；签名算法 SHA1(jsapi_ticket=X&noncestr=X&timestamp=X&url=X) 按字典序；[wxSdk.ts](../frontend/src/services/wxSdk.ts) 动态加载 jweixin-1.6.0.js + 调用签名接口执行 wx.config；[QuestionnaireFillPage.tsx](../frontend/src/pages/Public/QuestionnaireFillPage.tsx) source 上报修复（微信环境 source='wechat'）；未配置凭证时 503 降级为仅 UA 检测。新增 8 个 F-004 + 5 个 E-002 = 13 个测试用例；BUG-003 同步修复（SHA1 签名参数顺序 + 测试票面数据错误）。整体进度 84.3% → 87.1%，M2 90% → 95%，M3 96% → 98%，M4 70% → 73% | - |

---

## 六、使用说明

1. **更新状态**：每完成一项功能，将对应行的状态字段由 ⬜/🟡 改为 ✅，并在"说明/验证依据"列填写代码位置或验证方式。
2. **新增功能**：若需求扩展，在对应模块下新增行，分配编号（模块前缀+三位数字）。
3. **进度计算**：完成率 = (✅数 + 0.5×🟡数) / 总功能数 × 100%，更新后同步刷新"一、总体进度概览"。
4. **里程碑同步**：每完成一个里程碑的关键交付物，更新"四、里程碑达成情况"。
